apiVersion: v1
kind: Service
metadata:
  name: templateing-service
spec:
  selector:
    app: templateing-service
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3000
  type: LoadBalancer
